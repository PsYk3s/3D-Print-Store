<!DOCTYPE html>
<html lang="en">
<%- include('partials/head', {title: product.name + ' - 3D Print Store' }) %>
    <body>
        <%- include('partials/header') %>
            <main class="product-detail">
                <div class="single-product-img"><img src="<%= product.image %>" alt="<%= product.name %>"></div>
                <div>
                    <h1><%= product.name %></h1>
                    <p><%= product.description %></p>
                    <p>Price: R<%= product.price %></p>
                </div>
            </main>
            <%- include('partials/footer') %>
    </body>
</html>